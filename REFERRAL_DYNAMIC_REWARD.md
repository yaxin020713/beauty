# 推薦系統 - 動態返利實裝

基於您 products 和 orders 表的新增欄位，推薦系統已升級為**動態返利計算**。

## ✨ 新功能

### 動態返利計算
- 每個商品有獨立的「返利」欄位（products 表）
- 訂單返利 = Σ(商品數量 × 商品返利)
- 自動計算無需手動輸入

### 完整推薦追踪
Orders 表現在自動記錄：
- ✅ `推薦碼` - 購買時的推薦碼
- ✅ `推薦人信箱` - 推薦者的郵箱
- ✅ `返利` - 該訂單產生的返利金額

## 🔄 完整流程

```
用戶點擊推薦鏈接 (?ref=A1B2C3D4)
    ↓
推薦碼保存到 localStorage
    ↓
用戶下單
    ↓
API /api/orders 接收推薦碼
    ↓
查詢推薦碼 → 找到推薦人信箱 ✓
    ↓
遍歷訂單品項：
  - 查詢每個商品的「返利」字段
  - 計算: 返利 = Σ(數量 × 返利)
    ↓
Orders 表記錄:
  - 推薦碼 = A1B2C3D4
  - 推薦人信箱 = referrer@example.com
  - 返利 = 計算出的金額
    ↓
Members 表更新:
  - 推薦人的「累積返利」+= 計算的返利
```

## 📊 數據結構

### Products 表（新增）
```
| 商品名稱 | 價格 | 返利 |
|---------|------|------|
| 小黑瓶  | 1200 | 50   |
| 面膜    | 800  | 30   |
| 精油    | 600  | 20   |
```

### Orders 表（新增字段）
```
| Order_ID  | Customer_Email | 推薦碼   | 推薦人信箱        | 返利 | Items |
|-----------|----------------|---------|------------------|------|-------|
| 訂單-123  | buyer@ex.com   | A1B2C3D4| referrer@ex.com  | 80   | 小黑瓶x1,面膜x1 |
```

### Members 表（自動更新）
```
| Email            | 推薦碼   | 累積返利 |
|------------------|---------|---------|
| referrer@ex.com  | A1B2C3D4| 150     |
```

## 💻 API 邏輯

### POST /api/orders

**輸入**（from CheckoutModal）
```json
{
  "customerEmail": "buyer@example.com",
  "referralCode": "A1B2C3D4",
  "items": [
    { "productId": "prod-1", "quantity": 1, ... },
    { "productId": "prod-2", "quantity": 2, ... }
  ]
}
```

**內部處理**
1. ✅ 驗證推薦碼（查詢 members 表）
2. ✅ 獲取推薦人信箱
3. ✅ 遍歷訂單品項，查詢每個商品的返利字段
4. ✅ 計算總返利 = Σ(數量 × 返利)
5. ✅ 保存訂單記錄（包含推薦碼、推薦人信箱、返利）
6. ✅ 更新推薦人的累積返利

**輸出**
```json
{
  "success": true,
  "orderId": "訂單-1692345600000",
  "totalPrice": 2000,
  "referralCode": "A1B2C3D4",
  "referrerEmail": "referrer@example.com",
  "referralReward": 80
}
```

## ⚙️ 配置說明

### 1. Products 表設置
在 products 表中添加「返利」欄位（數字類型）

```
商品1: 返利 = 50
商品2: 返利 = 30
商品3: 返利 = 0  # 暫不返利
```

### 2. Orders 表設置
已自動支持：
- `推薦碼` (文字)
- `推薦人信箱` (文字)
- `返利` (文字)

### 3. Members 表設置
已自動支持：
- `累積返利` (數字) - 會自動累加

## 🧪 測試場景

### 場景1：單個商品訂單
```
購買: 商品A (返利50) x1
→ 計算: 50 × 1 = 50
→ 推薦人獲得: NT$50
```

### 場景2：多個商品訂單
```
購買: 
  - 商品A (返利50) x1 = 50
  - 商品B (返利30) x2 = 60
→ 計算: 50 + 60 = 110
→ 推薦人獲得: NT$110
```

### 場景3：返利為0的商品
```
購買:
  - 商品A (返利50) x1 = 50
  - 商品C (返利0) x3 = 0
→ 計算: 50 + 0 = 50
→ 推薦人獲得: NT$50
```

## 📝 字段對應

| Notion 表 | 字段名 | 欄位名 | 說明 |
|----------|--------|--------|------|
| Products | 返利 | 返利 | 該商品的返利金額 |
| Orders | 推薦碼 | 推薦碼 | 購買時的推薦碼 |
| Orders | 推薦人信箱 | 推薦人信箱 | 推薦者的郵箱 |
| Orders | 返利 | 返利 | 該訂單的總返利 |
| Members | 累積返利 | 累積返利 | 推薦人的總返利 |

## 🔍 監控返利計算

### 在 Notion 中檢查
1. 打開 Orders 表
2. 查看最新訂單的「推薦碼」、「推薦人信箱」、「返利」
3. 打開 Members 表，驗證推薦人的「累積返利」是否增加

### API 日誌檢查
```
開發工具 → Console
查看 /api/orders 的響應，驗證 referralReward 值
```

### 關鍵日誌消息
```javascript
// 成功示例
{
  referralCode: "A1B2C3D4",
  referrerEmail: "referrer@example.com",
  referralReward: 110
}

// 失敗日誌（如果有）
[api/orders] 無法查詢商品的返利: ...
[api/orders] 無法更新推薦人的返利: ...
```

## 🎯 邏輯驗證清單

- [ ] Products 表所有商品都有「返利」字段
- [ ] 返利字段值為數字（包括0）
- [ ] Orders 表有「推薦碼」、「推薦人信箱」、「返利」欄位
- [ ] Members 表有「累積返利」數字欄位
- [ ] 推薦人信箱正確對應 Members 表的 email

## 💡 常見問題

**Q: 如果商品沒有返利字段怎麼辦？**
A: API 會自動跳過，使用 0 計算。建議所有商品都設置返利字段。

**Q: 返利只支持數字嗎？**
A: 是的。Products 表的返利字段必須是數字類型。

**Q: 如何修改已存在訂單的返利？**
A: 在 Orders 表中手動編輯「返利」欄位。Members 表的「累積返利」需要手動調整。

**Q: 如果沒有推薦碼但有推薦人信箱怎麼辦？**
A: Orders 表會記錄，但 Members 表不會自動更新。只有通過推薦碼進來的訂單才會更新。

**Q: 推薦人信箱錯誤會怎樣？**
A: Members 表更新會失敗，API 會記錄警告，但不影響訂單創建。

## 📈 後續優化

1. **批量返利計算**
   - 定時任務計算未結算的訂單
   - 支持手動重新計算

2. **返利審核**
   - 在 Orders 表添加「返利審核」欄位
   - 只有審核通過的訂單才更新推薦人

3. **返利詳情**
   - 創建「推薦詳情」表，記錄每筆推薦
   - 支持推薦人查看詳細推薦記錄

4. **自動結算**
   - 達到最小返利額度時自動轉賬
   - 支持返利兌換商品

---

**實裝日期**: 2025年8月18日  
**構建狀態**: ✅ 通過  
**返利計算方式**: 動態（基於商品返利字段）
