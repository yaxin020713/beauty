# 管理員面板文檔

## 概覽

管理員面板提供完整的訂單和銷售管理功能。只有管理員（yaxinzhu2002@gmail.com）可以訪問此面板。

## 功能

### 1️⃣ 銷售統計 (Statistics Tab)

#### 統計卡片
- **商品總數**：系統中所有商品的數量
- **銷售總數**：所有商品的總銷售單位數
- **總銷售額**：所有訂單的總金額

#### 商品排行榜
- 按銷售量倒序排列
- 顯示商品名稱、分類、售價
- 銷售數量和銷售額
- **進度條視覺化**：直觀展示相對銷售量
- 可滾動查看所有商品

#### 日期範圍篩選
- 選擇開始日期和結束日期
- 點擊「篩選」按鈕重新計算統計
- 支持查看特定時間段的銷售數據

### 2️⃣ 訂單管理 (Orders Tab)

#### 訂單列表
- 顯示所有訂單，按訂單ID倒序
- 每個訂單顯示：
  - 訂單 ID
  - 客戶名稱和電話
  - 訂單金額
  - 訂單狀態標籤（新訂單/已出貨/已取消）
  - 付款狀態標籤（待核帳/已核帳/已退款）

#### 搜尋功能 🔍
- 按訂單 ID、客戶名稱、電話號碼搜尋
- 即時搜尋，無需按下確認按鈕

#### 狀態過濾
- 快速過濾按鈕：全部、新訂單、已出貨、已取消
- 一鍵切換篩選條件

#### 訂單詳情
- 點擊訂單卡片展開查看完整信息：
  - 商品明細（商品名稱和數量）
  - 總重量（kg）
  - 匯款末五碼
  - 建立時間

#### 訂單狀態編輯 ✏️
- 點擊「編輯狀態」按鈕
- 下拉菜單選擇新狀態：
  - **訂單狀態**：新訂單 → 已出貨 → 已取消
  - **付款狀態**：待核帳 → 已核帳 / 已退款
- 點擊「保存」確認更改
- 更新後自動重新載入頁面

#### 匯出功能 📥
- 點擊「匯出 CSV」按鈕
- 導出當前視圖中的所有訂單（考慮搜尋和過濾）
- CSV 文件包含：訂單ID、客戶、電話、商品、金額、重量、狀態、付款狀態
- 文件名格式：`訂單匯出_YYYY-MM-DD.csv`
- 可在 Excel、Google Sheets 等應用中打開

## 訂單狀態流程

### 訂單狀態
```
新訂單 → 已出貨 → （完成）
    ↘ 已取消
```

### 付款狀態
```
待核帳 → 已核帳 → （完成）
    ↘ 已退款
```

## 顏色指示

### 訂單狀態
- 🔵 **新訂單** (藍色) - 剛建立的訂單，尚未處理
- 🟢 **已出貨** (綠色) - 已發送給客戶
- 🔴 **已取消** (紅色) - 訂單已取消

### 付款狀態
- 🟡 **待核帳** (黃色) - 等待確認付款
- 🟢 **已核帳** (綠色) - 付款已確認
- 🔴 **已退款** (紅色) - 款項已退回

## 數據來源

所有數據來自 Notion 數據庫：

| 端點 | 功能 |
|------|------|
| GET `/api/admin/statistics` | 獲取銷售統計和商品排行 |
| GET `/api/admin/orders` | 獲取所有訂單 |
| PATCH `/api/admin/orders/[orderId]` | 更新訂單狀態 |

## 使用流程

### 日常訂單處理
1. 打開「管理面板」
2. 進入「訂單管理」標籤
3. 查看「新訂單」過濾的訂單
4. 確認付款後，點擊訂單的「編輯狀態」
5. 將「付款狀態」改為「已核帳」
6. 準備發貨時，將「訂單狀態」改為「已出貨」

### 查看銷售報表
1. 進入「銷售統計」標籤
2. （可選）設定日期範圍進行篩選
3. 查看統計卡片瞭解整體銷售情況
4. 查看商品排行榜瞭解熱銷商品
5. 點擊「匯出 CSV」下載詳細數據

### 搜尋特定訂單
1. 進入「訂單管理」標籤
2. 在搜尋框輸入：
   - 訂單 ID (例如：訂單-1692345678)
   - 客戶名稱 (例如：王小明)
   - 電話號碼 (例如：09123456789)
3. 匹配的訂單會自動篩選顯示

## 最佳實踐

✅ **應該做**
- 定期檢查「新訂單」並及時處理
- 每天核帳一次，確保訂單狀態準確
- 使用日期篩選分析不同時期的銷售
- 定期匯出訂單備份

❌ **不要做**
- 不要手動編輯 Notion 數據庫中的數據（使用面板編輯）
- 不要同時修改同一訂單（避免數據衝突）
- 不要清除已完成的訂單（保留歷史記錄）

## 故障排除

### 更新訂單後沒有反應
- 刷新頁面重新載入數據
- 檢查網路連接
- 確認訂單 ID 正確

### 匯出的 CSV 顯示亂碼
- 在 Excel 中使用「文字匯入精靈」
- 選擇編碼為 UTF-8
- 分隔符號選擇「逗號」

### 統計數字不准確
- 確保所有訂單已正確建立
- 檢查商品的 `Total_Sold` 欄位
- 清除瀏覽器緩存後重新載入

## API 參考

### GET /api/admin/statistics
獲取銷售統計和商品排行。

**查詢參數**
- `startDate` (可選): YYYY-MM-DD
- `endDate` (可選): YYYY-MM-DD

**響應示例**
```json
{
  "success": true,
  "summary": {
    "totalProductCount": 15,
    "totalUnitsSold": 248,
    "totalRevenue": 125000,
    "startDate": "全部時間",
    "endDate": "全部時間"
  },
  "products": [
    {
      "id": "page-id",
      "name": "小黑瓶精華液",
      "category": "護膚",
      "price": 1500,
      "totalSold": 45
    }
  ]
}
```

### GET /api/admin/orders
獲取所有訂單。

**查詢參數**
- `status` (可選): 訂單狀態
- `limit` (可選): 返回數量限制（默認100）

**響應示例**
```json
{
  "success": true,
  "total": 10,
  "orders": [
    {
      "id": "page-id",
      "orderId": "訂單-1692345678",
      "customerName": "王小明",
      "customerPhone": "09123456789",
      "paymentLast5": "12345",
      "itemsDetail": "小黑瓶 x2, 白繃帶 x1",
      "totalPrice": 3500,
      "totalWeightKg": 0.15,
      "status": "新訂單",
      "paymentStatus": "待核帳",
      "createdTime": "2026-08-16T12:00:00.000Z"
    }
  ]
}
```

### PATCH /api/admin/orders/[orderId]
更新訂單狀態。

**請求體**
```json
{
  "status": "已出貨",
  "paymentStatus": "已核帳"
}
```

**響應**
```json
{
  "success": true,
  "message": "訂單已更新"
}
```

## 隱私和安全

- 只有管理員可以訪問面板
- 所有 API 調用都使用安全的 HTTP 方法
- 訂單數據存儲在 Notion，不保存在前端
- 定期備份重要數據

## 相關文檔

- [登入系統](./AUTH.md) - 用戶認證和角色管理
- [API 訂單](./app/api/orders/route.ts) - 客戶訂單建立
- [購物車](./CART_PERSISTENCE.md) - 購物車管理
